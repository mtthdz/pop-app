import { StyleSheet } from "react-native";

export const SCF = StyleSheet.create({
  mainContainer: {
    flexDirection: 'row',
    flexWrap: 'nowrap',
    justifyContent: 'space-between',
    alignItems: 'center',
    borderWidth: 1,
    paddingVertical: 20
  }
})